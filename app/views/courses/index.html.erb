<body>
<h1 id="welcomehead"></h1>
<div class ="col-lg-2 text-center"><a class="btn btn-info" role="button" onclick="startLogout();">Log Out</a></div>
<h1>Courses</h1>
<div>
  <%= link_to 'Create New Course', new_course_path, :class => "btn btn-success", :style => "color:black"%>
</div>

<p id="notice"><%= notice %></p>
<table id="dt" class="table table-striped table-bordered table-sm" cellspacing="0" width="100%">
  <thead>
    <tr>
      <th>Course Name</th>
      <th>Professor</th>
      <th colspan="2">Actions</th>
    </tr>
  </thead>

  <tbody>
    <% @courses.each do |course| %>
      <tr>
        <td style="padding: 5px">
            <%= link_to course.name, course, :class => "btn btn-link" %>
        </td>
        <td><%= course.prof %></td>
        <td style="padding: 5px;">
            <%= link_to 'Edit Course Info', edit_course_path(course), :class => "btn btn-warning" %>
        </td>
        <td style="padding: 5px;">
            <%= button_to 'Delete Course', course, :class => "btn btn-danger", method: :delete, data: {confirm: 'Are you sure?'} %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
<br>

</body>

